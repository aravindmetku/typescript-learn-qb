import _ = require("lodash");
import {Object} from "../index";

// Common types are already defined in common.d.ts
declare module "../index" {
  // find 
  interface LoDashStatic {
    find<T, S extends T>(collection: List<T> | undefined | null, predicate?: ListIteratorTypeGuard<T, S>, fromIndex?: number): S | undefined;
    find<T>(collection: List<T> | undefined | null, predicate?: ListIterateeCustom<T, boolean>, fromIndex?: number): T | undefined;
    find<T extends object>(collection: T, predicate?: ObjectIterateeCustom<T, boolean>, fromIndex?: number): T | undefined;
  }

  // _.size
  interface LoDashStatic {
  }

  // _.includes
  interface LoDashStatic {
  }

  // _.countBy
  interface LoDashStatic {
  }

  // _.map
  interface LoDashStatic {
  }
}
